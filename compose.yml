---
include:
  - frontend/compose.yml
  - backend/compose.yml
services:
  nginx:
    image: nginx:1.29.8
    restart: unless-stopped
    ports:
      - 80:80
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - frontend
      - backend
