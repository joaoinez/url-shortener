---
services:
  backend:
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      APP_ENV: development
    volumes:
      - ./database.db:/app/database.db
    ports:
      - 8000:8000
