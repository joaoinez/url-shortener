---
services:
  backend:
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      APP_ENV: production
    volumes:
      - ./database.db:/app/database.db
